<%= form_with(model: person) do |form| %>
  <%= render '/shared/errors', object: person %>
  <div class="field">
    <%= form.label :fullname %>
    <%= form.text_field :fullname %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
