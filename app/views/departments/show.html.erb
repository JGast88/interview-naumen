<p id="notice"><%= notice %></p>

<p>
  <strong>Created at:</strong>
  <%= @department.created_at %>
</p>

<p>
  <strong>Disbanded at:</strong>
  <%= @department.disbanded_at %>
</p>

<p>
  <strong>Name:</strong>
  <%= @department.name %>
</p>

<p>
  <strong>Parent:</strong>
  <%= @department.parent_id %>
</p>

<%= link_to 'Assign person to department', new_working_period_path(department: @department) %> |
<%= link_to 'New parent', new_parent_department_path(@department) %> |
<%= link_to 'New name', new_name_department_path(@department) %> |
<%= link_to 'Edit', edit_department_path(@department) %> |
<%= link_to 'Back', departments_path %>

<h2>Actions</h2>

<%= render 'form_set_name', department: @department %>