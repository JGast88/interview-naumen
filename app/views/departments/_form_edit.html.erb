<%= form_with(model: department) do |form| %>
  <%= render '/shared/errors', object: department %>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.datetime_select :created_at %>
  </div>

  <div class="field">
    <%= form.label :disbanded_at %>
    <%= form.datetime_select :disbanded_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
